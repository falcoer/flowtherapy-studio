# Prototype mode

Current focus: **PROTO-01 viability**.

The project is temporarily optimized for one end-to-end proof rather than roadmap completeness:

`Brand → Library → Campaign → Events/content → Template → Creative direction → Resolved scene → PNG export`

See `docs/prototype-viability.md` and `docs/prototype-viability-checklist.md`.
